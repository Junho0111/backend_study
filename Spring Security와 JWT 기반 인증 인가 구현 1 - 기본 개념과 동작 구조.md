# Spring Security와 JWT 기반 인증 인가 구현 1 - 기본 개념과 동작 구조

프로젝트를 진행하면서 새롭게 배우고 적용한 부분에 대해서 다시 한번 보고 정리를 하고자 작성하였습니다.
<br>
**Spring Security**가 무엇인지, **필터**가 어떻게 동작하는지, 그리고 **JWT**의 구조(페이로드 등)와 동작 원리에 대해 정리하였습니다.

<br>

## 1. Spring Security와 필터

### Spring Security란?
Spring Security는 스프링 기반 애플리케이션의 인증과 인가를 담당하는 보안 프레임워크입니다.
이는 개발자가 일일이 보안 로직을 짜지 않아도, 방대한 보안 기능(비밀번호 암호화, CSRF 방어, 세션 관리 등)을 기본적으로 제공합니다.

### 필터란?
스프링 부트 애플리케이션에 클라이언트의 요청이 들어오면, 그 요청이 핵심 비즈니스 로직에 도달하기 전에 여러 개의 관문을 거치게 됩니다. 이 관문들을 **필터**라고 부릅니다.
> 참고: 필터는 DispatcherServlet으로 요청이 넘어가기전에 먼저 거침.

Spring Security는 이러한 필터들이 여러 개 연결된 **필터 체인(Filter Chain)** 형태로 동작합니다.

> 시큐리티에는 개발자가 직접 필터를 만들어서 추가하거나 새로 추가할 수 도있지만 기본적으로 들어있는 필터가 약 15개 정도입니다.

<br>

## 2. JWT (JSON Web Token) 란?

Spring Security는 기본적으로 서버의 메모리에 상태를 유지하는 **세션** 방식을 사용하지만, 무상태 환경에서는 **JWT**를 이용합니다. (이것은 상황에 따라 다릅니다.)

### JWT의 구조
JWT는 클라이언트와 서버 간에 정보를 안전하게 전달하기 위해 JSON 객체를 암호화한 토큰입니다. 긴 문자열 형태로 되어 있으며, `.`을 기준으로 세 부분으로 나뉩니다. (`Header.Payload.Signature`)

1. **Header (헤더)**
   토큰의 타입(JWT)과 서명에 사용된 암호화 알고리즘 정보(예: HS256)가 담겨 있습니다.
2. **Payload (페이로드)**
   실제로 서버가 클라이언트에게 전달하고자 하는 정보들(**Claim**)이 담겨 있습니다.
   - 예: 토큰 만료 시간, 발급자, 사용자의 고유 ID(memberId), 사용자의 권한(role) 등
   - **주의:** 페이로드는 암호화되는 것이 아니라 누구나 알아볼 수 있게 인코딩(Base64)만 되어 있습니다. (비밀번호 같은 민감한 정보는 절대 넣으면 안됨.)
3. **Signature (서명)**
   해당 토큰이 조작되거나 위조되지 않았음을 증명합니다. 헤더와 페이로드를 합친 후, 서버만 알고 있는 **비밀키**를 사용해 해싱하여 만듭니다.
   - 만약 누군가 페이로드의 권한(role)을 'USER'에서 'ADMIN'으로 몰래 조작하더라도, 서버가 자신이 가진 비밀키로 다시 계산한 서명과 토큰에 적힌 서명이 서로 다르게 되므로 조작된 토큰임을 즉시 알아채고 요청을 거부할 수 있습니다.

<br>

## 3. JWT 기반 인증의 전체적인 구조와 흐름

스프링 시큐리티 필터와 JWT가 결합되었을 때의 구조는 다음과 같습니다.

1. **로그인 시 (토큰 발급):**
   사용자가 아이디와 비밀번호를 보내면, 서버는 DB를 확인해 인증합니다. 성공하면 사용자의 핵심 정보를 페이로드에 담은 **JWT**를 생성하여 응답으로 내려줍니다. (민감한 정보는 절대X)
2. **API 요청 시 (토큰 검증):**
   클라이언트가 글 쓰기, 내 정보 보기 등의 API를 요청할 때마다 HTTP 헤더(Authorization: Bearer <JWT 토큰 문자열>)에 방금 받은 JWT를 담아서 보냅니다.
3. **커스텀 필터의 동작:**
   Spring Security의 수많은 필터 중 직접 설정한 JWT 인증 필터(JwtAuthenticationFilter)가 이 토큰을 낚아챕니다.
   - 서버의 비밀키로 토큰의 서명을 검증하여 위조 여부를 확인합니다.
   - 위조되지 않았고 만료되지 않았다면, 페이로드에서 사용자 식별 정보를 꺼냅니다.
   - 이 정보를 **SecurityContext**(스프링 시큐리티가 현재 이 요청을 보낸 사람이 누구인지를 임시로 기억하는 전용 보관소)에 담아둡니다.
   >요청 헤더 + 요청 페이로드 + 서버내의 비밀키의 조합이 요청 토큰과 다르면 다른것임.
4. **로직 수행:**
   필터를 무사히 통과한 요청은 디스패처서블릿을 지나 컨트롤러로 들어가고, 개발자는 필요할 때 SecurityContext에서 유저 정보를 꺼내 안전하게 비즈니스 로직을 처리할 수 있습니다.(응답후에는 다시 비움)

<br>