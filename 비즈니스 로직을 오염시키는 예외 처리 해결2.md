# 비즈니스 로직을 오염시키는 예외 처리 해결하기2

프로젝트를 진행하면서 새롭게 배우고 적용한 부분에 대해서 다시 한번 보고 정리를 하고자 작성하였습니다.
<br>
이 글은 서비스 내에서 발생하는 예외들을 어떻게 처리하였으며, 또한 클라이언트에게 일관된 형태의 에러 응답을 제공하는지에 대한 구조를 어떻게 설계했는지 정리하였습니다.
<br>

## 1. 비즈니스 로직을 오염시키는 예외 처리와 응답 비표준화 문제

### 문제 상황
서비스를 개발하다 보면 비즈니스 로직 상의 에러(예: "이미 사용 중인 이메일", "권한 없음")나, 사용자 입력 검증 실패(`@Valid` 등) 같은 다양한 예외 상황이 발생합니다.
만약 각 컨트롤러나 서비스 메서드마다 `try-catch`로 예외를 잡아서 처리하거나, 응답 형태(JSON)가 다 다르다면 클라이언트 입장에서 에러 처리를 공통화하기가 매우 어렵습니다.
또한 서버 개발자 입장에서도 로그를 남기는 방식과 에러를 반환하는 코드가 중복해서 발생하는 문제가 생기게됩니다.

### 해결
스프링이 제공하는 `@RestControllerAdvice`와 공통 에러 응답 객체 `ErrorResponse`를 사용하고, 예외 발생 시 던질 상위 예외 클래스(`BadRequestException`, `ConflictException` 등)를 정의하여 도메인별로 정의 내린 예외를 상속받아 처리하면 되게되는것 입니다.
> 이를 통해 도메인별 예외는 상위 예외 클래스를 상속받아 세부 내용을 정의하기만 하면 되고, 예외가 발생하면 전역 예외 처리기(`GlobalControllerAdvice`)가 이를 일괄적으로 잡아 처리하도록 책임을 명확히 분리함.

<br>

## 2. 패턴 구현 방법

### 1. 상위 커스텀 예외 클래스 정의 (`BadRequestException.java`, `ConflictException.java` 등)
`RuntimeException`을 상속받아 HTTP 상태 코드에 대응되는 상위 예외 클래스를 생성합니다.
>이 상위 예외클래스는 앞으로의 도메인들이 가져다가 사용할 비즈니스 예외이며, 각 도메인에 맞게 좀 더 구체화하여 사용하는것임.

클라이언트에게 보여줄 메시지(`clientMessage`)와 서버 로그용 상세 메시지(`serverMessage`), 그리고 프론트와 약속된 자체 식별 코드(`errorCode`)를 관리합니다.

```java
public class ConflictException extends RuntimeException {

    private String errorCode = "CONFLICT";
    private String clientMessage = "리소스 충돌이 발생했습니다.";

    public ConflictException(String serverMessage, String clientMessage, String errorCode) {
        super(serverMessage); 
        this.clientMessage = clientMessage;
        this.errorCode = errorCode;
    }

    public String getErrorCode() {
        return errorCode;
    }

    public String getClientMessage() {
        return clientMessage;
    }
}
```

### 2. 도메인별 하위 커스텀 예외 클래스 구현 (`DuplicateEmailException.java` 등)
상위 예외인 `ConflictException` 등을 상속받아, 실제 도메인 로직에서 발생하는 구체적인 예외를 정의합니다.
이렇게 하면 추상화하였던 비즈니스 예외의 의미가 명확해지고, 상황에 맞는 상세 서버 메시지와 전용 에러 코드를 지정할 수 있습니다.

```java
public class DuplicateEmailException extends ConflictException {

    private static final String ERROR_CODE = "AUTH_001";
    private static final String SERVER_MESSAGE = "회원가입 이메일 중복 발생";
    private static final String CLIENT_MESSAGE = "이미 사용 중인 이메일입니다.";

    public DuplicateEmailException(String email) {
        super(String.format("%s -> email: %s", SERVER_MESSAGE, email), CLIENT_MESSAGE, ERROR_CODE);
    }
}
```

### 3. 표준화된 에러 응답 객체 (`ErrorResponse.java`)
예외가 발생했을 때 클라이언트로 보내는 JSON 응답 스펙을 규격화합니다. 어떠한 에러가 나더라도 항상 `{ "code": "...", "message": "..." }` 형태를 유지하게 됩니다.

```java
@Getter
public class ErrorResponse {

    private final String code;
    private final String message;

    public ErrorResponse(String code, String message) {
        this.code = code;
        this.message = message;
    }
}
```

### 4. 전역 예외 처리기 (`GlobalControllerAdvice.java`)
`@RestControllerAdvice`를 통해 전역에서 발생하는 예외를 낚아채서 처리합니다. 최상위 예외 클래스(`BadRequestException`, `ConflictException`) 단위로 `@ExceptionHandler`를 등록하고, `@ResponseStatus`를 통해 HTTP 상태 코드를 매핑합니다.

```java
@Slf4j
@RestControllerAdvice
public class GlobalControllerAdvice {

    private static final String ERROR_LOGGING_MESSAGE = "예외 발생: ";

    @ResponseStatus(HttpStatus.BAD_REQUEST)
    @ExceptionHandler
    public ErrorResponse handleBadRequestException(BadRequestException e) {
        log.error(ERROR_LOGGING_MESSAGE, e);
        return new ErrorResponse(e.getErrorCode(), e.getClientMessage());
    }

    @ResponseStatus(HttpStatus.CONFLICT)
    @ExceptionHandler
    public ErrorResponse handleConflictException(ConflictException e) {
        log.error(ERROR_LOGGING_MESSAGE, e);
        return new ErrorResponse(e.getErrorCode(), e.getClientMessage());
    }

    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    @ExceptionHandler
    public void handleRuntimeException(RuntimeException e) {
        log.error("예상하지 못한 에러가 발생하였습니다.", e);
    }
}
```

<br>

## 3. 이 패턴의 장점 (비즈니스 로직 적용 시)

도메인 로직이나 데이터 검증 실패 시, 구체적인 도메인 예외를 `throw` 하기만 하면 모든 응답 처리가 끝납니다.

**[적용 예시]**
```java
public void checkEmailDuplicate(String email) {
    if (memberRepository.existsByEmail(email)) {
    
        throw new DuplicateEmailException(email);
    }

    // 예외만 던지면 GlobalControllerAdvice가 낚아채서 
    // 409 Conflict 와 함께
    //  {
    //    "code": "AUTH_001",
    //    "message": "이미 사용 중인 이메일입니다."
    //   }
    // 형태의 JSON 응답을 만들어줍니다.
}
```

<br>

1. 예외 구조를 계층화하여 공통 속성 처리를 상위 예외에 두고 도메인별 예외를 구체화함으로써, 코드가 깔끔해지고 유지보수가 매우 편리해집니다.
2. 예외가 발생하더라도 클라이언트에게 언제나 예측 가능한 규격화한 구조(`ErrorResponse`)의 JSON 에러 데이터를 전달할 수 있습니다.
3. 비즈니스 로직에서 에러 응답을 만들거나 로깅하는 `try-catch` 코드가 사라져 핵심 로직 구현에만 집중할 수 있게 됩니다.

<br>

## +추가 정보

### 그래서 어떤 패턴을 선택해야 할까?

예외처리 1편과 2편을 통해 두 가지 예외 처리 패턴을 정리해 보았는데, 각각 장단점이 있지만, 실무에서는 보통 1편에서 다룬 첫 번째 패턴(Enum 기반)을 더 많이 사용합니다. 

두 번째 패턴처럼 도메인별로 하위 예외 클래스를 만들게 되면, 서비스 규모가 커지고 에러 상황이 늘어날수록 많은 예외 클래스 파일이 생성되는 문제가 발생하고 또 관리가 매우 힘들어 집니다.    

그에반해, 첫 번째 패턴을 사용하면 예외 클래스는 하나로 통일하고 모든 에러 상황을 ErrorCode Enum 한 곳에서 일관되게 관리할 수 있어 유지보수를 하기에도 편하며, 새로 추가하는데도 부담이 없습니다.
