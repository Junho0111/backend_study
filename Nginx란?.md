# Nginx 정리 글

Nginx는 현재 Web Server, Reverse Proxy, Load Balancer 등 여러 가지 기능을 지원하며 웹서버 분야에서 1등 자리를 유지하있는데

앞으로 이 Nginx를 사용하는 데 있어서, 탄생 이유와 이렇게 1등 자리에 오르기까지의 과정을 정리해둘 필요성을 느껴 해당 글을 작성하게 되었습니다.

<br>

## 목차

- [1. 기존 아파치 HTTP 서버의 구조와 작동 방식](#1-기존-아파치-http-서버의-구조와-작동-방식)
- [2. 트래픽 증가와 C10K 문제](#2-트래픽-증가와-c10k-문제)
- [3. Nginx초기 사용 용도](#3-nginx초기-사용-용도)
- [5. Nginx의 성능과 장점](#5-nginx의-성능과-장점)
- [6. 아파치 서버와 Nginx의 현재](#6-아파치-서버와-nginx의-현재)
- [7. Nginx의 추가 기능](#7-nginx의-추가-기능)

<br>

## 1. 기존 아파치 HTTP 서버의 구조와 작동 방식

기존에 있던 아파치 HTTP 서버는 요청이 들어오면 커넥션을 형성하기 위해 프로세스를 생성합니다. 그래서 아파치 HTTP 서버는 새로운 클라이언트 요청이 들어올 때마다 새로운 프로세스를 만드는데, 이는 unix 계열의 OS가 네트워크 커넥션을 형성하는 모델을 그대로 적용한 것입니다.

그런데 프로세스를 만드는 작업이 시간이 좀 걸리다 보니, 아파치 HTTP 서버는 요청이 들어오기 전에 프로세스를 미리 만들어두는 prefork 방식을 사용합니다. 그래서 새로운 클라이언트로부터 요청이 들어오면 미리 만들어 놓은 프로세스를 가져다 사용하는데 만약 만들어놓은 프로세스가 모두 할당되었다면 추가로 프로세스를 만드는 식으로 동작합니다.

### 아파치 HTTP 서버의 장점

위와 같은 구조는 개발하기 쉽다는 장점이 있습니다.
>확장성이 좋음(다양한 기능을 만들어서 서버에 기능을 추가하기 좋음)

이렇게 아파치 HTTP 서버는 동적으로 서버를 처리하기 쉽고 확장성이 좋다는 장점 덕분에, 요청을 받고 응답을 처리하는 과정을 하나의 서버에서 해결하기 좋습니다. 그리고 이러한 이유들은 Nginx이전에 아파치 HTTP 서버가 1등 자리를 유지하던 이유였습니다.

<br>

## 2. 트래픽 증가와 C10K 문제

문제는 이후 트래픽이 증가하는 시대에서 생기게됩니다. 이전에는 서버가 처리해야 할 요청 양이 그 당시 기술로 감당 가능한 수준이었으나, 점점 컴퓨터가 많이 보급되면서 요청이 많아지고 서버에 문제가 생기기 시작했습니다.

해당 문제는 서버에 동시에 연결된 커넥션이 많아졌을 때 더 이상 커넥션을 형성하지 못한다는것인데, 이를 C10K 문제라고 하며, connection 10000 problem이라고도 합니다. 클라이언트 수가 많아지면 그만큼 동시에 연결되어 있는 커넥션 수가 많아질 것이고, 그렇게 커넥션 수가 10000단위를 넘어가는 순간 서버는 더 이상 커넥션을 형성하지 못한다는 것 입니다.

### 아파치 HTTP 서버 구조의 한계와 Nginx 등장

당시 아파치 HTTP 서버 구조로는 이 문제를 해결하기 어려웠습니다. 어차피 HTTP 서버는 구조상 커넥션이 형성될 때마다 프로세스를 할당하는데, 이 때문에 동시에 처리하고 있는 커넥션이 많아지면 그만큼 형성된 프로세스가 많다는 것이고, 이는 곧 메모리 부족 현상으로 이어지기 때문입니다. 거기에 아파치 서버의 장점인 '여러 기능을 쉽게 추가할 수 있는 것'은, 프로세스가 차지하는 리소스양을 늘린다는 단점으로 다가오기 시작했습니다. 그리고 많은 커넥션 수가 들어오기 시작하면 컨텍스트 스위칭이 수차례 일어나며 CPU에 부하가 걸리는 문제까지 생기게되었습니다.

물론 아파치에서는 여러 방법으로 해결하려고 했지만, 이때 이런 아파치 HTTP 서버의 문제를 보완하는 소프트웨어가 나오게 되었는데, 이것이 바로 cginx입니다.

<br>

## 3. Nginx초기 사용 용도

원래 Nginx는 아파치 서버와 함께 사용하며, 아파치 서버 앞단에서 수많은 동시 커넥션을 대신 유지해주는 용도 사용되었습니다. 즉 구조적으로 동시 커넥션을 유지하기 어려운 아파치 서버의 단점을 메우며 같이 사용되었으며, 거기에 Nginx는 그 자체로 웹서버이기 때문에 정적 파일에 대한 요청은 스스로 처리했습니다. 그리고 동적 파일 요청을 받을 때만 뒤의 아파치 서버와 커넥션을 연결하여 처리하는 식으로 사용되었습니다.

<br>

## 4. Nginx는 어떻게 수많은 동시 요청을 감당 할 수 있는가?

구조가 다르기 때문입니다.

기본적으로 마스터 프로세스가 있고, 설정 파일을 읽고나서 워커 프로세스를 생성합니다. 그리고 이 워커 프로세스가 실제로 일을 수행하게됩니다. 워커 프로세스는 만들어질 때 각자 지정된 listen 소켓을 배정받습니다. 그리고 그 소켓에 새로운 클라이언트로부터 요청이 들어오면 커넥션을 형성하고 그 요청을 처리하는데 그 커넥션은 정해진 keep alive 시간만큼 유지합니다.

하지만 커넥션이 형성되었다고 해서 워커 프로세스가 해당 커넥션 하나만 처리하지는 않습니다. 형성된 커넥션에서 아무런 요청이 없으면 새로운 커넥션을 형성하거나, 이미 만들어진 다른 커넥션으로부터 들어온 요청을 처리하는 효율적인 방식으로 수행합니다.

Nginx에서는 이런 커넥션 생성/제거, 새로운 요청을 처리하는 것을 이벤트라고 부르며, 이 이벤트들은 OS 커널이 큐 형태로 워커 프로세스에게 전달해줍니다. 그리고 이 이벤트는 큐에 담긴 상태에서 비동기 방식으로 대기하며, 앞에 무거운(시간이 오래 걸리는) 작업이 있으면 따로 스레드풀을 만들어 위임을 하여 따로 처리하게끔 하고, 다시 돌아가 진행하던 일을 처리합니다.

그리고 이런 워커 프로세스는 보통 CPU의 코어 수만큼 생성하며, 이러한 방식은 코어가 담당하는 프로세스를 바꾸는 횟수를 줄일 수 있습니다. 즉 CPU의 부하를 높이는 작업(컨텍스트 스위칭)을 하지 않습니다.
>만약 프로세스가 코어수보다 많다면 컨텍스트 스위칭 작업이 빈번히 일어나 CPU에 부하를 주게됨.

그리고 이것이 아파치 서버와는 다른 구조인 이벤트 기반 구조입니다.

### Nginx 구조의 단점

하지만 장점만 있는 건 아니고 단점도 있습니다. 개발자가 기능을 추가하려다가 돌아가고 있는 워커 프로세스를 종료하는 상황이 생길 수 있다는 것인데, 이러한 이유로 Nginx는 개발자가 직접 모듈을 만들기가 까다롭게 되어있습니다.

<br>

## 5. Nginx의 성능과 장점

하지만 단점에 비해 장점이 매우 명확하고 컷습니다. 동시 커넥션 양이 최소 10배 이상 증가하였으며(일반적으로는 100배에서 1000배 증가라고 함), 동일한 커넥션 수일 때 처리 속도가 2배 이상 향상되었습니다. 그리고 이렇게 프로세스를 적게 만드는 구조 덕분에 가볍기까지 했습니다.

### Nginx의 동적 설정 변경

프로세스를 적게 만드는 이 구조는 Nginx의 설정을 동적으로 바꾸는 것을 가능하게 합니다. 개발자가 설정 파일을 변경하고 Nginx에 해당 설정을 적용하면, 마스터 프로세스가 그 설정에 맞는 워커 프로세스를 따로 생성하고 기존의 워커 프로세스가 더 이상 커넥션을 형성하지 않도록 합니다. 즉 기존에 담당하던 구버전의 워커 프로세스의 이벤트가 모두 끝나면 해당 구버전의 워커 프로세스를 종료하게됩니다.

이러한 동적 설정 변경은, Nginx가 여러 동시 요청을 받아내고 있는 상황에서 뒷단에 서버가 추가되는 상황일 때(이때부터는 Nginx가 Load Balancer 역할도 담당함. Reverse Proxy기능이 사라지는 것이 아님.) 유용합니다. 설정을 바꾸기 위해 Nginx를 종료하기에는 무리가 있지만, 위와 같이 동적으로 설정을 바꾼다면 동시 커넥션을 유지하면서 기존 요청을 계속 처리하고 뒷단에 서버를 추가할 수 있습니다.

그리고 이 또한 이벤트 기반 구조라서 가능한 방식입니다.

<br>

## 6. 아파치 서버와 Nginx의 현재

하지만 아파치 서버도 지금까지 오면서 꾸준한 업데이트를 하며 1등, 2등을 다투고 있는 상황이며, 또한 아파치 서버 자체가 이전에 여러 버그 문제를 해결하려 나온 것이고, 그만큼 호환성과 확장성이 좋고 또 여러 OS에서 안정적이라는 장점이 있습니다. 그리고 추가할 수 있는 모듈 또한 Nginx보다 훨씬 많다는 장점이 있습니다. 

Nginx는 윈도우와 같은 OS에서는 제대로 된 성능을 발휘하지 못한다는 단점이 있습니다.

따라서 이러한 장/단점과 해당 소프트웨어가 생겨나게된 배경과 이유를 알면, 왜 이 서버를 선택해야 하는지 알 수 있습니다.
> (현재는 휴대폰, 컴퓨터 등 여러 요청 수를 처리해야 하는 대요청 처리 시대이니만큼, Nginx가 현대에서 1등을 하는 시대적인 배경 이유가 있음.)

<br>

## 7. Nginx의 추가 기능

Nginx는 동시 커넥션을 여러 개 유지할 수 있고 그 자체로 가볍다는 장점을 살려서 웹서버 가속기 역할도 수행합니다.

### 1. SSL 터미네이션

Nginx는 SSL 터미네이션을 수행할 수 있는데, 이때 SSL 터미네이션이란, Nginx가 클라이언트와는 HTTPS로 통신하고 서버와는 HTTP로 통신하여 서버가 복호화 과정을 처리하지 않도록 리소스를 줄여주는 것입니다. 그리고 보통 Nginx와 서버는 같은 네트워크 안에 있는 경우가 많기 때문에 HTTP 통신을 해도 보안적 위험이 비교적 적습니다.(만약 같은 네트워크가 아니라면 확인을 따로 해봐야함)

### 2. 캐싱

두 번째로 Nginx는 HTTP 프로토콜을 사용하여 전달하는 콘텐츠를 캐싱할 수 있습니다. 이 경우는 Nginx를 반대로 클라이언트와 가깝게 배치하며, 한 번 서버로부터 받은 응답을 캐싱하고 클라이언트에게 전달한다.

### 3. 기타 기능

이 외에도 HSTS(HTTP Strict Transport Security), CORS, TCP/UDP 커넥션 부하 분산, HTTP/2 등 여러 방식으로 서버를 지원한다.
