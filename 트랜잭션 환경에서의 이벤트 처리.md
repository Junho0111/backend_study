# @TransactionalEventListener를 활용해 트랜잭션 내에서 이벤트 처리하기

<br>

## 1. 문제 상황

사용자가 특정 장소(POI)에 대해 자신의 위시리스트에 담거나 취소할수있고,
위시리스트에 장소가 추가 또는 삭제 될때마다, 장소에도 자신이 담긴 횟수(wishCount) 값도 같이 증가하거나 감소해야하는 상황입니다.

<br>

만약 이를 해결하기위해 위시리스트를 생성하는 서비스 로직 안에서 `poiRepository.increaseWishCount()`를 직접 주입받아 호출한다면, **위시리스트 도메인**이 **장소 도메인**에 결합되는 문제가 발생합니다. 그리고 이러한 문제는 위시리스트 생성 로직이 장소 로직까지 짊어져 무거워지고, 유지보수성이 떨어지게 됩니다.

<br>

## 2. 해결 방법: 이벤트 발행(Publish)과 구독(Listen)
도메인 간의 강한 결합을 느슨히 하기위해 **스프링의 이벤트 발행/구독**을 사용했습니다.
이를 사용하게되면, 위시리스트 로직은 단순히 "위시리스트가 생성되었다"는 이벤트만 던지면서 자기 할 일을 끝내며, 이때 POI 로직이 그 이벤트를 바라보고 있다가 `wishCount`를 증가시켜줍니다.

### 1) 이벤트를 발행하는 쪽 (위시리스트 서비스)
스프링이 제공하는 `ApplicationEventPublisher`를 주입받아 이벤트를 발생시킵니다.
```java
private final ApplicationEventPublisher eventPublisher; 

@Transactional
public void createWishlist(Long memberId, Long poiId, Boolean isPublic) {
    // 존재 여부 확인 (POI)
    if (!poiRepository.existsById(poiId)) {
        throw new BusinessException(ErrorCode.POI_NOT_FOUND, "poiId=" + poiId);
    }

    // 중복 체크
    if (wishlistRepository.findByMemberIdAndPoiId(memberId, poiId).isPresent()) {
        throw new BusinessException(ErrorCode.WISHLIST_ALREADY_EXISTS, 
                "memberId=" + memberId + ", poiId=" + poiId);
    }

    // 1. 위시리스트 DB 저장 로직 처리
    wishlistRepository.save(Wishlist.builder()
            .memberId(memberId)
            .poiId(poiId)
            .isPublic(isPublic)
            .build());

    // 2. 위시리스트가 생성되었다는 이벤트를 발행 (POI 쪽으로 던짐)
    eventPublisher.publishEvent(new WishlistCreatedEvent(poiId));
}
```

<details>
<summary><b>WishlistEvent 코드 보기 (클릭)</b></summary>

```java
@Getter
public class WishlistDeletedEvent {
    private final Long poiId;

    public WishlistDeletedEvent(Long poiId) {
        this.poiId = poiId;
    }
}
```

```java
@Getter
public class WishlistDeletedEvent {
    private final Long poiId;

    public WishlistDeletedEvent(Long poiId) {
        this.poiId = poiId;
    }
}
```
</details>


<br>
<br>

### 2) 이벤트를 받아서 처리하는 쪽 
```java
@Slf4j
@Component
@RequiredArgsConstructor
public class PoiWishCountEventHandler {

    private final PoiRepository poiRepository;

    // 발행된 WishlistCreatedEvent를 낚아채 POI의 wishCount를 증가시킴
    @TransactionalEventListener(phase = TransactionPhase.BEFORE_COMMIT)
    public void onWishlistCreated(WishlistCreatedEvent event) {
        poiRepository.increaseWishCount(event.getPoiId());
    }

    @TransactionalEventListener(phase = TransactionPhase.BEFORE_COMMIT)
    public void onWishlistDeleted(WishlistDeletedEvent event) {
        poiRepository.decreaseWishCount(event.getPoiId());
    }
}
```

<br>

## 3. 왜 @EventListener가 아니라 @TransactionalEventListener일까요?

만약 `@EventListener`를 사용했다면, `eventPublisher.publishEvent()`가 호출되는 그 즉시 리스너 로직이 실행되어 버립니다. 

### @EventListener 
- 이벤트가 발행되는(publish) 즉시 실행됩니다.
- 트랜잭션 상태와 무관하게 동작하며, 기본적으로 발행 측의 트랜잭션 범위 내에서 동기적으로 실행됩니다.
- 따라서 이는 트랜잭션 성공 여부와 상관없이 즉시 처리해야 하는 부가 작업에 적합합니다.

<br>

하지만 장소의 위시 횟수(wishCount)를 갱신하는 일은 **위시리스트 생성이 DB에 정상적으로 커밋될 것이 확실해졌을떄 한 트랜잭션내에서 처리되어야합니다.**

### @TransactionalEventListener(phase = TransactionPhase.BEFORE_COMMIT)
- 트랜잭션의 특정 단계(TransactionPhase)에 맞춰 이벤트 처리를 바인딩합니다.
- 트랜잭션의 상태(커밋, 롤백 등)를 인식합니다
- phase 옵션 종류 
<br> 1) AFTER_COMMIT (기본값): 트랜잭션이 성공적으로 커밋된 후에 이벤트를 실행합니다.
<br> 2) AFTER_ROLLBACK: 트랜잭션이 롤백된 경우에만 실행합니다.
<br> 3) AFTER_COMPLETION: 커밋이든 롤백이든 트랜잭션이 종료된 후 실행합니다.
<br> 4) BEFORE_COMMIT: 트랜잭션 커밋 직전에 실행합니다.
>실행 중인 트랜잭션이 없다면 기본적으로 이벤트가 무시되므로(fallbackExecution = true로 변경하여 @EventListener처럼 사용가능)

이를 통해 코드 상으로는 도메인 간의 의존성을 분리하면서, 한 트랜잭션 내에서 원자적으로 처리할 수 있게 되었습니다.
