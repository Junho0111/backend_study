# 엔티티의 생성/수정 시간 자동 관리

대부분의 DB 테이블에는 데이터가 언제 생성되었고, 언제 마지막으로 수정되었는지를 기록하는 컬럼이 필수적으로 들어갑니다. 
<br>
이를 엔티티마다 매번 중복으로 선언하거나 서비스 로직에서 직접 시간을 갱신하지 않고, **JPA Auditing 기능과 BaseTimeEntity를 조합하여 자동으로 관리**하는 방법에 대해 정리하였습니다.

<br>

## 1. EnableJpaAuditingConfig 설정
먼저 스프링 부트 엔진에 시간을 감시(Auditing)하는 기능을 활성화해야 합니다.

```java
@Configuration
@EnableJpaAuditing // JPA Auditing 기능 활성화
public class EnableJpaAuditingConfig {
}
```
- `@EnableJpaAuditing`: 엔티티가 생성되거나 수정될 때 시간을 자동으로 기록해 주는 JPA Auditing 기능 입니다.

### 왜 메인 클래스(application)에 안 적고 이 설정을 별도로 분리했을까요?
보통 메인 애플리케이션 클래스(`@SpringBootApplication`이 있는 클래스)에 `@EnableJpaAuditing`을 함께 붙이는 경우가 있습니다.
<br>
하지만 이렇게 하면 웹 계층만 따로 `@WebMvcTest` 같은 테스트를 진행할 때, JPA 관련 빈(DB 등)들이 로드되지 않은 상태에서 Auditing 객체만 켜지려고 하므로 에러가 발생하게 됩니다.
따라서 위와 같이 `EnableJpaAuditingConfig`라는 설정 클래스로 따로 분리해 해야하며, 이는 JPA 관련 테스트(`@DataJpaTest` 등)나 실제 서버를 띄울 때만 선택적으로 로드할 수 있다는 장점이 있습니다.

> **참고 (전역 빈 설정)**: 별개 내용이지만 이와 비슷하게 외부 통신을 위한 `RestClient` 등의 범용 빈들도 별도의 `AppConfig` 클래스를 만들어 깔끔하게 모아서 관리합니다.
> ```java
> @Configuration
> public class AppConfig {
>     @Bean
>     public RestClient restClient() { return RestClient.create(); }
> }
> ```

<br>

## 2. BaseTimeEntity 정의
EnableJpaAuditing를 활성화 했으면, 이제 데이터베이스에 생성일과 수정일을 기록할 공통 컬럼을 모아둔 클래스를 만들어야 합니다.

```java
@Getter
@MappedSuperclass
@EntityListeners(AuditingEntityListener.class)
public class BaseTimeEntity {

    @CreatedDate
    @Column(name = "created_at", nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @LastModifiedDate
    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
}
```

### 적용된 애노테이션의 역할
- **`@MappedSuperclass`**: 이 클래스를 상속받는 자식 엔티티(Poi, Member 등)에게 자신의 필드(`createdAt`, `updatedAt`)를 실제 DB 컬럼으로 매핑시켜 내려주는 공통 부모 역할을 합니다.
- **`@EntityListeners(AuditingEntityListener.class)`**: JPA 엔티티에 이벤트(생성, 수정)가 발생할 때마다 해당 이벤트를 감지하고 Listener코드(AuditingEntityListener.class)를 실행시켜 시간을 자동으로 기록해주는 스프링 데이터 JPA의 감시자(Listener) 입니다. 
- **`@CreatedDate` / `@LastModifiedDate`**: 엔티티가 DB에 최초로 `INSERT` 될 때, 그리고 `UPDATE` 될 때 각각의 필드에 현재 시간을 자동으로 채워 넣습니다.
- **`updatable = false`**: 생성 시간(`createdAt`)은 처음 데이터가 생성된 이후 절대 수정되어서는 안 되므로, JPA가 UPDATE 쿼리를 날릴 때 이 컬럼은 무시합니다

<br>

## 3. 엔티티에서의 활용 (Poi.java)
이제 실제 도메인 엔티티에서 `BaseTimeEntity`를 상속받기만 하면 알아서 생성시간과 업데이트 시간을 추적하는 컬럼이 추가됩니다.

```java
@Entity
public class Poi extends BaseTimeEntity { // 상속
    
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    // createdAt, updatedAt은 상속받아 자동으로 포함되며 관리됨
    
    // ... 나머지 필드 생략 ...
}
```

<br>

### 정리
결국 개발자가 서비스 로직에서 직접 `poi.setCreatedAt(LocalDateTime.now())`를 매번 호출하거나 여러 도메인 엔티티에 공통 컬럼을 추가 할 필요없이
`EnableJpaAuditingConfig`가 엔티티의 생성 수정 등의 이벤트를 감시하고, `BaseTimeEntity`가 변화를 감지해 자신의 필드에 시간을 기록해줍니다.
