# 마우스 클릭부터 서버까지의 과정 정리

클라이언트로부터 3-way handshake를 통해 연결을 맺고 request를 받는다는 두루뭉실한 과정이 아닌 정말 제대로 정리해두고싶어서 정리해두는 정리글.

<br>

## 전체적인 순서

|||
|---|---|
| 1 | 물리적 클릭 ➡️ 브라우저 소프트웨어 |
| 2 | 브라우저의 논리적 요청 ➡️ OS 네트워크 스택 (소켓) |
| 3 | 커널 안의 패킷 ➡️ 물리적 랜카드(NIC)와 공유기 |
| 4 | 홈 라우터(공유기) ➡️ 인터넷 망 |
| 5 | 통신사 에지 라우터 ➡️ 목적지 서버 데이터센터 |
| 6 | 서버 랜카드(NIC) ➡️ 서버 OS 소켓 수신 버퍼 |
| 7 | 커널 소켓 버퍼 ➡️ 리버스 프록시 (Nginx) |
| 8 | Nginx 전달 ➡️ Tomcat의 HttpServletRequest 탄생 |

---

<br>

## 1. 물리적 클릭 ➡️ 브라우저 소프트웨어

사용자가 마우스 버튼을 누르는 순간, 브라우저 내부의 자바스크립트 엔진이 이를 인식하는 **소프트웨어 이벤트**로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — 하드웨어 인터럽트와 V8 엔진</b></summary>
  <div>
    <br>

1. 마우스 하드웨어의 스위치가 닫히며 전기적 신호가 발생하고, 이 신호는 메인보드를 타고 CPU로 향합니다.
2. CPU에는 **하드웨어 인터럽트**가 발생합니다. CPU는 하던 일(컨텍스트)을 저장하고 OS의 인터럽트 핸들러를 호출하여 하드웨어 이벤트를 읽어냅니다.
3. **OS**는 화면의 프로그램이 브라우저임을 인지하고, 브라우저 프로세스로 이 이벤트를 던져줍니다.
4. 브라우저의 메인 렌더러 스레드가 이를 감지하면, 내장된 **V8 자바스크립트 엔진**의 이벤트 루프와 콜 스택에 클릭 이벤트 핸들러를 올립니다.
5. JS 코드 내에 작성된 `fetch('https://api.example.com/data')`가 실행되면, 메인 스레드는 블로킹을 방지하기 위해 이 네트워크 통신 작업을 브라우저의 **백그라운드 네트워크 스레드**로 위임합니다.

  </div>
</details>

<br>

➡️ 도착한 브라우저 네트워크 스레드에서, 이제 데이터를 서버로 보내기 위한 준비가 시작됩니다.

---

## 2. 브라우저의 논리적 요청 ➡️ OS 네트워크 스택 (소켓)

브라우저가 알고 있는 도메인 주소(`api.example.com`) 기반의 HTTP 요청이, OS 커널 안의 네트워크 스택을 거쳐 **TCP/IP 패킷**으로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — DNS 쿼리와 커널 네트워크 스택 진입</b></summary>
  <div>
    <br>

1. 네트워크 스레드는 먼저 브라우저/OS 캐시를 뒤져 IP를 찾고, 없다면 OS를 통해 통신사(ISP)의 **DNS 서버**로 UDP 질의를 보내 서버의 IP 주소(예: `192.0.2.1`)를 알아옵니다.
2. IP를 획득한 브라우저는 OS 커널에게 **시스템 콜**을 보내 TCP 소켓 생성을 요청합니다. OS는 메모리에 소켓(통신의 출발점 역할을 하는 파일/구조체)을 만들고, 브라우저의 HTTP 텍스트 데이터를 OS 커널 영역으로 복사해 가져옵니다.
3. **TCP 계층 (전송 계층):** 데이터는 조각으로 나뉘고, 443(HTTPS) 목적지 포트와 시퀀스 번호가 담긴 TCP 헤더가 씌워집니다.
4. **IP 계층 (네트워크 계층):** 출발지 IP(내 PC)와 목적지 IP(`192.0.2.1`)가 담긴 IP 헤더가 덧붙여져 IP 패킷이 완성됩니다.

  </div>
</details>

<br>

➡️ 도착한 OS 커널의 하단부(데이터 링크 계층 진입 직전)에서, 패킷은 PC 밖으로 나가기 위해 물리적인 주소를 찾는 과정을 거치게됩니다.

---

## 3. 커널 안의 패킷 ➡️ 물리적 랜카드(NIC)와 공유기

OS 메모리상에만 존재하던 디지털 패킷이 **물리적인 형태**(전기/빛)로 변환되어, 첫 번째 목적지인 집 안의 라우터(공유기)로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — ARP 브로드캐스트와 DMA 전송</b></summary>
  <div>
    <br>

1. 서버 IP(`192.0.2.1`)는 외부망에 있으므로, OS는 패킷을 밖으로 전달해 줄 **기본 게이트웨이**(우리 집 공유기, 예: `192.168.0.1`)로 1차 목적지를 설정합니다.
2. 공유기의 물리적 MAC 주소를 알아내기 위해 OS는 **ARP**(주소 분해 프로토콜)를 실행합니다.
>`192.168.0.1` 아이피 가진 기기를 찾고 MAC 주소를 받는 브로드캐스트 메시지를 집 안 전체 네트워크에 뿌리고, 공유기로부터 MAC 주소 응답(유니캐스트)을 받아옵니다.
3. 이제 목적지 공유기의 MAC 주소가 담긴 **이더넷 프레임**(MAC 헤더)을 패킷에 최종적으로 씌웁니다. 프레임은 메인 메모리의 송신 링 버퍼에 쌓입니다.
4. CPU가 전송 명령을 내리면, 랜카드(NIC)는 **DMA** 기술을 써서 CPU를 거치지 않고 메모리의 프레임을 자신의 버퍼로 빼옵니다.
5. 랜카드는 이를 전기 신호로 변환하여 랜선을 통해 공유기로 쏘아 보냅니다.

  </div>
</details>

<br>

➡️ 도착한 공유기(라우터)에서, 사설망(집)의 신분이었던 패킷은 인터넷 세상으로 나가기 위한 준비를 합니다.

---

## 4. 홈 라우터(공유기) ➡️ 인터넷 망

사설 IP를 달고 있던 패킷이 공유기에서 변환 과정을 거쳐, 전 세계망으로 뻗어 나가는 통신사(ISP) 라우터로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — NAT/NAPT와 캡슐화 해제 및 재포장</b></summary>
  <div>
    <br>

1. 공유기의 랜카드가 전기 신호를 다시 이더넷 프레임으로 복원하고 커널로 올립니다. 공유기는 이더넷 헤더를 벗겨보고 해당 패킷은 인터넷으로 나가는구나 라고 라우팅 판단을 합니다.
2. **NAT(Network Address Translation) 수행:** 패킷의 출발지인 내 PC의 사설 IP(`192.168.0.10`)와 포트를 통신사가 공유기에게 부여한 단 하나의 공인 IP와 새로운 랜덤 포트로 교체합니다.
3. 돌아올 때를 대비해 공유기 내부 메모리의 **NAT 테이블** 장부에 이 맵핑 기록(사설 IP:포트 ↔ 공인 IP:포트)을 적어둡니다.
4. IP와 포트 번호가 바뀌었으므로 패킷이 훼손된 것으로 오해받지 않도록 헤더의 **체크섬** 값을 재계산하여 덮어씁니다.
5. 공유기 역시 ARP를 통해 집 밖 전봇대에 있는 통신사(ISP) 라우터의 MAC 주소를 알아내고, 새로운 MAC 헤더를 씌워 WAN 포트를 통해 전송합니다.

  </div>
</details>

<br>

➡️ 도착한 통신사(ISP) 망에서, 패킷은 바다를 건너 목적지 서버를 찾아가게됩니다.

---

## 5. 통신사 에지 라우터 ➡️ 목적지 서버 데이터센터

국내 통신사 망에 진입한 패킷이 **BGP**(경계 경로 프로토콜)의 안내를 받아 해저 케이블을 횡단하여 목적지 서버 데이터센터로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — BGP 최적 경로 탐색과 Hop-by-Hop 라우팅</b></summary>
  <div>
    <br>

1. 통신사 내부망을 초고속으로 통과한 패킷은 인터넷의 국경인 **BGP 라우터**에 도착합니다.
2. BGP 라우터는 전 세계 라우팅 지도를 훑어보고, 목적지 IP(`192.0.2.1`)가 속한 AS(Autonomous System, 예: AWS 망)로 가기 위해 가장 저렴하고 짧은 **AS-PATH**(거쳐야 할 통신망 개수)를 계산해 다음 목적지를 결정합니다.
3. 거대한 라우터들을 지날 때마다 **IP 헤더(최종 목적지)는 절대 변하지 않지만**, **MAC 헤더는 매번 다음 라우터의 주소로 바뀌면서 새로 붙여지고 진행됩니다.** 
>패킷의 무한 루프를 막기 위해 지날 때마다 **TTL 값**은 1씩 감소합니다.<br>
TTL(Time To Live): 패킷이 네트워크를 떠돌아다니다가 영원히 사라지지 않고 무한 루프에 빠지는 걸 막기 위한 안전장치. <br>이름은 생존 시간이지만 실제로는 시간이 아니라 거쳐갈 수 있는 라우터(Hop)의 최대 개수를 의미함.
4. 한국 망과 외국 망이 만나는 **IXP**(인터넷 교환 노드)를 거쳐, 빛 신호로 바뀐 패킷은 광케이블을 타고 횡단합니다.
5. 목적지에 도착하면, 기업 내부 라우팅 시스템(IGP)이 랙(Rack) 단위까지 패킷을 안내하여 최종적으로 서버 장비가 물려있는 **ToR 스위치**에 도달합니다.

  </div>
</details>

<br>

➡️ 도착한 데이터센터 서버 기기의 물리적 랜카드(NIC)에서, 패킷은 다시 전기 신호에서 소프트웨어의 영역인 서버 OS 커널로 올라갈 준비를 합니다.

---

## 6. 서버 랜카드(NIC) ➡️ 서버 OS 소켓 수신 버퍼

물리적 랜카드에 도달한 신호가 서버 CPU의 인터럽트를 거쳐, 리눅스 OS 커널의 네트워크 스택을 뚫고 해당 포트를 열어둔 프로그램의 메모리 공간(수신 버퍼)으로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — NAPI 폴링과 네트워크 스택 역진입</b></summary>
  <div>
    <br>

1. 서버 랜카드가 신호를 0과 1의 이더넷 프레임으로 복원하여 자신의 메모리에 담고, CPU에 하드웨어 인터럽트를 발생시킵니다.
2. CPU는 랜카드 버퍼를 폴링하며 DMA를 통해 커널 메모리로 데이터를 퍼 올립니다.
3. 커널 메모리로 올라온 데이터는 역순으로 껍질을 벗습니다. **MAC 헤더**를 검사해 버리고, **IP 헤더**를 검사해 라우팅 확인 후 버립니다.
4. **TCP 헤더**를 열어 시퀀스 번호를 통해 패킷의 순서를 맞추고, 도중에 유실된 패킷이 있다면 재요청합니다.
5. 조립이 완료된 데이터 덩어리(암호화된 텍스트)는 443번 포트를 열고 기다리던 소켓의 **수신 버퍼**에 쌓입니다.

  </div>
</details>

<br>

➡️ 도착한 OS 커널의 소켓 수신 버퍼에서, 데이터는 유저 스페이스에서 기다리고 있는 웹 서버 프로그램으로 넘어가게 됩니다.

---

## 7. 커널 소켓 버퍼 ➡️ 리버스 프록시 (Nginx)

OS 메모리에 쌓인 암호화된 바이트 조각들이 Nginx에 의해 복호화되고 분석되어, 뒷단의 서버( Spring Boot 애플리케이션)로 넘어가게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — epoll 이벤트 루프와 SSL/TLS 복호화</b></summary>
  <div>
    <br>

1. 소켓 수신 버퍼에 데이터가 쌓이는 순간, 리눅스 커널의 **시스템 콜**이 작동하여 대기 상태로 잠들어있던 Nginx의 Worker 프로세스를 깨웁니다.
2. Nginx는 논블로킹 방식으로 소켓에서 데이터를 퍼 올립니다. 이 데이터는 아직 암호화되어 있으므로, Nginx는 서버의 개인키를 사용해 데이터를 복호화합니다.
3. 이후 순수 HTTP 규격의 텍스트가 드러납니다. Nginx는 헤더를 파싱하여 URI(`/data`)를 확인합니다.
4. `nginx.conf`의 설정에 따라, Nginx는 이 요청을 처리할 백엔드 서버(도커 컨테이너 내부의 Tomcat, 보통 8080 포트)를 향해 새로운 TCP 소켓 연결을 맺고 순수 HTTP 텍스트를 그대로 전달합니다.

  </div>
</details>

<br>

➡️ 도착한 톰캣(Tomcat)이 리스닝하는 8080 포트에서, 프레임워크의 심장부로 진입하기 위한 마지막 작업이 시작됩니다.

---

## 8. Nginx 전달 ➡️ Tomcat의 `HttpServletRequest` 탄생

Nginx로부터 내부 네트워크를 거쳐 들어온 HTTP 텍스트 스트림이, Tomcat의 쓰레드들을 거쳐 마침내 Spring 프레임워크가 제어 할 수 있는 순수한 **Java 객체**로 변환되게 됩니다.

<details>
  <summary><b> 상세 과정 펼치기 (클릭) — Tomcat NIO 모델과 HTTP 파싱</b></summary>
  <div>
    <br>

1. Tomcat의 **Acceptor 스레드**가 8080 포트 서버 소켓에서 대기하다가, Nginx가 요청한 소켓 연결(Connection)을 맺어 줍니다.
2. 이 연결(Socket Channel)은 **Poller 스레드**로 넘어갑니다. Poller는 Java NIO의 `Selector`를 이용해 수천 개의 소켓 채널을 감시하다가, 진짜 HTTP 텍스트 데이터가 들어오는 순간을 포착하여 이벤트를 생성해 **Worker 스레드 풀(Thread Pool)** 큐에 던집니다.
3. 워커 스레드 하나가 이벤트를 가져가 소켓에서 바이트 배열을 읽습니다.
4. 내부의 **Coyote HTTP 파서**가 작동하여 이 텍스트 덩어리에서 HTTP Method(`GET`, `POST` 등), URI, 헤더들을 한 줄씩 잘라냅니다.
5. 파싱이 끝나면, 워커 스레드는 힙(Heap) 메모리에 Request 객체를 생성하여 이 데이터를 맵핑해 넣습니다.
6. 마지막으로, 스프링(Spring)과 같은 서블릿 기술이 이 객체를 안전하게 다룰 수 있도록 `HttpServletRequestFacade`라는 껍데기로 이 객체를 감쌉니다(Wrapping).

  </div>
</details>

<br>

➡️ `HttpServletRequest` 객체를 쥔 워커 스레드는, 이제 서블릿 **필터(Filter) 체인**을 통과하여 Spring의 입구인 **`DispatcherServlet`**으로 들어오게됩니다.

