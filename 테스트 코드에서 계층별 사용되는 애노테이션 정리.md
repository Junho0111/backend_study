# 계층별 사용되는 테스트 코드 애노테이션 정리

프로젝트를 진행하면서 작성한 테스트 코드를 계층별로 분리하고, 각 계층에 맞게 사용되는 애노테이션들을 어떻게 적용했는지 다시 한번 보고 정리를 하고자 작성하였습니다.
<br>
컨트롤러 테스트를 하는데 애플리케이션의 모든 빈을 다 띄우는 `@SpringBootTest`를 붙일 이유는 없습니다. 
<br>
이 글은 각 계층별 역할에 맞는 애노테이션을 사용하여 테스트 하는 방법에대해 정리하였습니다.

<br>

## 1. DB (Repository) 테스트

### 사용되는 애노테이션
- `@DataJpaTest`
- `@Import(...)`

### 왜 이 애노테이션들을 사용할까요?
Repository 계층은 실제 데이터베이스와의 상호작용을 테스트하는 것이 핵심입니다. 따라서 스프링 빈을 모두 로드할 필요 없이, JPA 관련 설정과 빈들만 로드하여 빠르고 가볍게 테스트를 진행해야 합니다.

- **`@DataJpaTest`**: JPA 관련 컴포넌트(DataSource, EntityManager 등)만 로드하여 테스트 환경을 구성합니다. 기본적으로 인메모리 데이터베이스로 교체되며, 각 테스트가 끝난 후 자동으로 롤백을 수행합니다.
- **`@Import`**: `@DataJpaTest`는 순수 JPA(Spring Data JPA) 관련 빈만 등록하기 때문에, Querydsl 설정 빈이나 Auditing(생성일/수정일 자동화) 빈은 로드되지 않습니다.
<br>
때문에 필요한 추가 설정 클래스가 있을 경우 `@Import`를 사용해 등록해줍니다.

### 예시 코드 (QPoiRepositoryTest.java)
```java
@Import({QPoiRepository.class, EnableJpaAuditingConfig.class}) // Querydsl과 Auditing 설정을 추가로 가져옴
@DataJpaTest // JPA 관련 설정만 로드하여 테스트
@Transactional
class QPoiRepositoryTest {
    
    @TestConfiguration
    static class QuerydslTestConfig {
        @PersistenceContext
        private EntityManager em;

        @Bean
        public JPAQueryFactory jpaQueryFactory() {
            return new JPAQueryFactory(em);
        }
    }
    
    @Autowired
    QPoiRepository qPoiRepository;
    
    // ... 테스트 로직 생략 ...
}
```

<br>

## 2. Service (비즈니스 로직) 테스트

### 사용되는 애노테이션
- `@ExtendWith(MockitoExtension.class)`
- `@InjectMocks`
- `@Mock`

### 왜 이 애노테이션들을 사용할까요?
서비스 계층은 애플리케이션의 핵심 비즈니스 로직을 담당합니다. 이 계층의 테스트는 외부 의존성(DB, 외부 API 등)에 영향을 받지 않고 **순수한 비즈니스 로직**에만 집중하여 검증해야 합니다. 그리고 이렇게 하므로써 스프링 컨테이너를 띄우지 않고, 가짜 객체(Mock)를 주입하여 단위 테스트를 진행합니다.

- **`@ExtendWith(MockitoExtension.class)`**: 스프링 컨테이너 없이 Mockito 프레임워크를 사용하여 테스트를 실행하게 해주며, 실행 속도가 매우 빠릅니다.
- **`@Mock`**: 실제 객체 대신 동작을 제어할 수 있는 가짜(Mock) 객체를 생성합니다. (예: 가짜 DB Repository)
- **`@InjectMocks`**: `@Mock`으로 생성된 가짜 객체들을 이 애노테이션이 붙은 대상(테스트할 진짜 서비스)에 자동으로 주입해 줍니다.

### 예시 코드 (AuthServiceTest.java)
```java
@ExtendWith(MockitoExtension.class) // 스프링을 띄우지 않고 Mockito만 사용
class AuthServiceTest {

    @InjectMocks
    AuthService authService; // 테스트할 진짜 서비스 (아래의 Mock들이 여기에 주입됨)

    @Mock
    MemberRepository memberRepository; // DB 조회를 하는 가짜 객체

    @Mock
    JwtTokenProvider jwtTokenProvider; // 토큰 발급을 하는 가짜 객체

    @Test
    void 이메일_중복_회원가입_시_예외_발생() throws Exception {
        // given (가짜 DB의 동작을 미리 정의)
        given(memberRepository.existsByEmail("test@test.com")).willReturn(true);

        // when & then
        assertThatThrownBy(() -> authService.signUp("test@test.com", "testPassword", "testNickName"))
                .isInstanceOf(BusinessException.class)
                .hasMessageContaining("이미 사용 중인 이메일입니다.");
    }
}
```

<br>

## 3. Controller 테스트

### 사용되는 애노테이션
- `@WebMvcTest(controllers = ...)`
- `@AutoConfigureMockMvc(addFilters = false)`
- `@MockitoBean` (과거의 `@MockBean`을 대체)

### 왜 이 애노테이션들을 사용할까요?
컨트롤러 테스트는 HTTP 요청과 응답, URL 매핑, 파라미터 바인딩 및 데이터 검증(`@Valid`) 등이 잘 동작하는지 확인합니다. 때문에 전체 스프링 환경을 띄울 필요 없이, 웹 레이어(Controller, Filter, ControllerAdvice 등)만 로드하여 테스트합니다.

- **`@WebMvcTest`**: 지정한 컨트롤러와 웹 관련 빈들만 스프링 컨테이너에 로드하여 가볍게 테스트 환경을 구성합니다.
- **`@AutoConfigureMockMvc`**: 실제 서버를 띄우지 않고 HTTP 요청을 모의(Mock)로 보낼 수 있는 `MockMvc` 객체를 자동 설정해 줍니다. `addFilters = false`를 통해 시큐리티 필터 등을 비활성화하여 순수 컨트롤러 로직만 집중적으로 테스트할 수도 있습니다.
- **`@MockitoBean`**: 컨트롤러가 의존하는 서비스 계층의 빈을 가짜(Mock)로 만들어 스프링 컨테이너에 교체 등록해 줍니다.

### 예시 코드 (AuthControllerTest.java)
```java
@WebMvcTest(controllers = AuthController.class) // 웹 관련 빈과 AuthController만 로드
@AutoConfigureMockMvc(addFilters = false) // 시큐리티 필터를 끄고 MockMvc 설정
class AuthControllerTest {

    @MockitoBean
    AuthService authService; // 컨트롤러가 사용하는 서비스를 가짜 빈으로 등록

    @Autowired
    MockMvc mockMvc; // HTTP 요청을 모의로 보내주는 객체 주입

    @Test
    void 회원가입_시_중복_이메일이면_409_반환() throws Exception {
        // given (가짜 서비스의 동작 정의)
        given(authService.signUp("중복@test.com", "testPassword", "테스터"))
                .willThrow(new BusinessException(ErrorCode.DUPLICATE_EMAIL));

        // when & then (HTTP 요청 및 상태 코드, JSON 응답 내용 검증)
        mockMvc.perform(post("/api/v1/auth/signup")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(objectMapper.writeValueAsString(body)))
                .andExpect(status().isConflict())
                .andExpect(jsonPath("$.code").value("AUTH_001"));
    }
}
```

<br>

## 4. 통합 테스트 (Integration Test)

### 사용되는 애노테이션
- `@SpringBootTest`
- `@AutoConfigureMockMvc`
- `@Transactional`

### 왜 이 애노테이션들을 사용할까요?
통합 테스트는 클라이언트의 HTTP 요청부터 필터, 컨트롤러, 서비스, 데이터베이스(DB)까지 실제 애플리케이션의 모든 구성 요소가 잘 연결되어 작동하는지 전체 흐름을 검증합니다.

- **`@SpringBootTest`**: 애플리케이션의 모든 스프링 빈을 컨테이너에 로드합니다. (실제 운영 환경과 비슷함)
- **`@AutoConfigureMockMvc`**: 컨트롤러 테스트와 마찬가지로 HTTP 요청을 보내기 위해 사용합니다. 이때 가짜 객체(Mock) 없이 실제 컨트롤러와 서비스를 거쳐 실제 DB까지 데이터가 도달합니다.
- **`@Transactional`**: 테스트 내에서 발생한 실제 DB 변경 사항(INSERT, UPDATE 등)을 테스트가 끝난 후 자동으로 롤백하여, 다음 테스트에 영향을 주지 않도록 격리해 줍니다.

### 예시 코드 (PoiIntegrationTest.java)
```java
@SpringBootTest // 모든 빈을 로드하여 실제 환경 구축
@AutoConfigureMockMvc // HTTP 통신을 위한 MockMvc 설정
@Transactional // 테스트 완료 후 DB 자동 롤백
class PoiIntegrationTest {

    @Autowired
    MockMvc mockMvc;

    @Autowired
    PoiRepository poiRepository; // 가짜가 아닌 진짜 DB 저장소 사용

    @Test
    void POI_피드_조회_정상_흐름() throws Exception {
        // (BeforeEach를 통해 데이터를 미리 넣어둠)
        
        // when & then: 실제 엔드포인트를 찔러서 DB에서 데이터를 잘 가져오는지 확인
        mockMvc.perform(get("/api/v1/pois")
                        .param("regionId", String.valueOf(depth2.getId()))
                        .param("category", "ATTRACTION"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.content[0].name", is("전주 한옥마을")));
    }
}
```

<br>

### TransactionTemplate를 사용하여 수동으로 트랜잭션을 제어하는 경우

통합 테스트 코드를 작성 또는 작성한 것을 보면 `@Transactional` 애노테이션 없이, 아래와 같이 수동으로 데이터를 삭제하고 ID(Auto-Increment)를 초기화하는 코드를 볼 수 있습니다

```java
    @Autowired
    private TransactionTemplate transactionTemplate;
    
    @Autowired
    private JdbcTemplate jdbcTemplate;

    @AfterEach
    void tearDown() {
        // 1. TransactionTemplate을 이용해 데이터를 일괄 삭제
        transactionTemplate.executeWithoutResult(status -> {
            wishlistRepository.deleteAllInBatch();
            poiRepository.deleteAllInBatch();
            regionRepository.deleteAllInBatch();
            memberRepository.deleteAllInBatch();
        });
        
        // 2. JdbcTemplate을 이용해 Auto-Increment ID를 다시 1로 리셋
        jdbcTemplate.execute("ALTER TABLE wishlist ALTER COLUMN id RESTART WITH 1");
        jdbcTemplate.execute("ALTER TABLE poi ALTER COLUMN id RESTART WITH 1");
        jdbcTemplate.execute("ALTER TABLE region ALTER COLUMN id RESTART WITH 1");
        jdbcTemplate.execute("ALTER TABLE member ALTER COLUMN id RESTART WITH 1");
    }
```

굳이 수동으로 `tearDown()`을 만들어주는 이유는 다음과 같습니다.

<details>
<summary><b>tearDown() 만든 이유 보기 (클릭)</b></summary>

1. **Auto-Increment(ID) 값은 롤백되지 않습니다.**  
   `@Transactional`을 붙이면 DB에 넣었던 데이터 자체는 테스트 종료 후 롤백되어 사라집니다. 하지만 데이터베이스의 자동 증가 ID(Auto Increment) 시퀀스는 롤백되지 않고 계속 증가합니다. 만약 테스트 코드에서 생성된 엔티티의 ID가 무조건 `1L`일 것이라고 가정하고 검증하고 있다면, 다른 테스트가 먼저 실행되었을 때 ID가 밀려서 테스트가 실패하게 됩니다. 따라서 `ALTER TABLE ... RESTART WITH 1`을 통해 시퀀스도 깔끔하게 1로 리셋해 주어야 각 테스트가 독립적으로 동작합니다.

2. **`@SpringBootTest(webEnvironment = RANDOM_PORT)` 환경에서의 롤백 불가 문제 해결**  
   `MockMvc`를 사용하는 환경에서는 `@Transactional`이 잘 동작하지만, 만약 `RANDOM_PORT`나 `DEFINED_PORT`를 사용하여 실제 웹 서버(Tomcat 등)를 띄우고 `RestAssured` 등으로 실제 HTTP 요청을 보내는 E2E 테스트를 할 때는 스레드가 분리됩니다. 테스트 코드를 실행하는 스레드와 HTTP 요청을 받아서 처리하는 웹 서버의 스레드가 다르기 때문에 트랜잭션이 하나로 묶이지 못하여 자동 롤백이 안되는 문제가 발생합니다. 이럴 때는 반드시 `@AfterEach`에서 직접 데이터를 지워주어야 합니다.

3. **`deleteAllInBatch()`와 `TransactionTemplate`을 사용해 불필요한 코드 제거 및 원자성 보장**  
   일반적인 `deleteAll()`은 엔티티를 하나씩 조회(SELECT)한 후 건건이 삭제(DELETE)하기 때문에 성능이 떨어집니다. 반면 `deleteAllInBatch()`는 `DELETE FROM table` 쿼리 한 방으로 데이터를 통째로 지우기 때문에 속도가  빠르고 불필요하게 일일이 데이터를 지울 필요가 없습니다. 또한, 여러 테이블을 지우는 작업을 `TransactionTemplate`으로 묶어 하나의 트랜잭션 안에서 처리하도록 하여 원자성을 보장할 수 있도록 합니다
</details>

<br>

execute은 반환값이 필요할 때, executeWithoutResult은 반환값이 필요 없을 때, 사용하며 위의 예제처럼 람다식을 사용하는 경우 람다 블록 안에서 예외를 던지지 않고 특정 조건에서 명시적으로 롤백하고 싶을 때 파라미터로 넘어오는 TransactionStatus를 사용합니다.

<br>

정리하면 @Transactional 대신 사용하는 경우는? 

<br>

**트랜잭션 범위를 수동으로 제어하는 경우**
<br>
메서드 전체에 @Transactional을 걸면 메서드가 끝날 때까지 DB 커넥션을 물고 있는데 이때 외부 API 통신이나 느린 파일 I/O가 섞여 있는 상황에서, 커넥션이 고갈될 수 있으므로 DB 작업 부분만 TransactionTemplate으로 감싸서 커넥션 풀 고갈을 방지 하려는 경우

<br>

**내부 메서드 호출을 하는 경우**
<br>
스프링의 @Transactional은 기본적으로 AOP 프록시 기반이라 같은 클래스 내의 다른 메서드를 호출할 경우 트랜잭션이 적용되지 않는데, 이때 TransactionTemplate를 사용하여 프록시와 상관없이 원하는 블록에 트랜잭션을 걸어 테스트 하는 경우

<br>

## + 테스트 DB 설정 방법

### 1. 인메모리 H2 DB로 설정하는 법
스프링 부트는 테스트 코드(`@DataJpaTest` 등)를 실행할 때, 클래스에 `h2` 데이터베이스 의존성이 존재하면 자동으로 실제 DB 연결을 가로채서 **인메모리 H2 데이터베이스**로 대체합니다.
따라서 `build.gradle`에 아래와 같이 의존성을 추가하고, 운영/개발용 환경과 테스트용 환경의 설정 파일을 분리해주면 됩니다.

```gradle
// build.gradle
dependencies {
    testImplementation 'com.h2database:h2'
}
```

<br>

**[메인 코드의 설정: `src/main/resources/application.yml`]**
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gollagolla?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: gollagolla
    password: gollagolla
```

<br>

**[테스트 코드의 설정: `src/test/resources/application.yml`]**
```yaml
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MYSQL # MySQL 문법 호환 모드
    username: sa
    password: 
```

위와 같이 `src/main`과 `src/test`의 설정 파일을 분리해두면, 애플리케이션을 실제로 실행할 때는 메인 설정이 작동하여 **MySQL DB**와 연결되고, 테스트 코드를 돌릴 때는 테스트 설정이 우선순위를 가져서 자동으로 **인메모리 H2 DB**와 연결됩니다.

