# 비즈니스 로직을 오염시키는 예외 처리 해결하기

프로젝트를 진행하면서 새롭게 배우고 적용한 부분에 대해서 다시 한번 보고 정리를 하고자 작성하였습니다.
<br>
이 글은 서비스 내에서 발생하는 예외들을 어떻게 처리하였으며, 또한 클라이언트에게 일관된 형태의 에러 응답을 제공하는지에 대한 구조를 어떻게 설계했는지 정리하였습니다.

<br>

## 1. 비즈니스 로직을 오염시키는 예외 처리와 응답 비표준화 문제

### 문제 상황
서비스를 개발하다 보면 비즈니스 로직 상의 에러(예: "이미 사용 중인 이메일", "권한 없음")나, 사용자 입력 검증 실패(`@Valid` 등) 같은 다양한 예외 상황이 발생합니다.
만약 각 컨트롤러나 서비스 메서드마다 `try-catch`로 예외를 잡아서 처리하거나, 응답 형태(JSON)가 다 다르다면 클라이언트 입장에서 에러 처리를 공통화하기가 매우 어렵습니다.
또한 서버 개발자 입장에서도 로그를 남기는 방식과 에러를 반환하는 코드가 중복해서 발생하는 문제가 생기게됩니다.

### 해결
스프링이 제공하는 `@RestControllerAdvice`와 공통 에러 응답 객체 `ErrorResponse`, 그리고 에러 코드를 일원화한 **`ErrorCode`** Enum을 사용하여, 예외 케이스를 한곳에 모아 확장에 유용하도록 하였습니다.
> 이를 통해 비즈니스 로직에서는 예외가 발생하면 단순히 `throw new BusinessException(...)`만 던지도록 처리하고, HTTP 응답 변환과 로깅은 모두 `GlobalControllerAdvice`에게 위임하여 책임을 명확히 분리하였음.

<br>

## 2. 패턴 구현 방법

### 1. 에러 코드 일원화 (`ErrorCode.java`)
애플리케이션 전체에서 발생할 수 있는 에러들을 한곳에 모아서 관리합니다.
에러 발생 시 응답할 HTTP 상태 코드(`HttpStatus`), 프론트와 약속된 자체 에러 식별 코드(`code`), 그리고 클라이언트에게 보여줄 메시지(`clientMessage`)를 정의합니다.
>아래에 상태코드, 에러 식별코드, 메시지 외에도 얼마든지 추가 및 줄일 수 있음.

```java
@Getter
public enum ErrorCode {
    DUPLICATE_EMAIL(HttpStatus.CONFLICT, "AUTH_001", "이미 사용 중인 이메일입니다."),
    INVALID_CREDENTIALS(HttpStatus.UNAUTHORIZED, "AUTH_003", "이메일 또는 비밀번호가 올바르지 않습니다."),
    // ... 기타 에러들 생략 ...
    INVALID_INPUT_VALUE(HttpStatus.BAD_REQUEST, "COMMON_001", "잘못된 입력 값입니다."),
    INTERNAL_SERVER_ERROR(HttpStatus.INTERNAL_SERVER_ERROR, "SYS_500", "서버 내부 오류가 발생했습니다.");

    private final HttpStatus httpStatus;
    private final String code;
    private final String clientMessage;
    
    ErrorCode(HttpStatus httpStatus, String code, String clientMessage) {
        this.httpStatus = httpStatus;
        this.code = code;
        this.clientMessage = clientMessage;
    }
}
```

<br>

### 2. 표준화된 에러 응답 객체 (`ErrorResponse.java`)
예외가 발생했을 때 클라이언트에게 보여주는 JSON 응답 스펙을 규격화합니다. 어떠한 에러가 나더라도 항상 `{ "code": "...", "message": "..." }` 형태를 유지하게 됩니다.

```java
@Getter
public class ErrorResponse {
    private final String code;
    private final String message;

    public ErrorResponse(String code, String message) {
        this.code = code;
        this.message = message;
    }

    public static ErrorResponse from(ErrorCode errorCode) {
        return new ErrorResponse(errorCode.getCode(), errorCode.getClientMessage());
    }
}
```

<br>

### 3. 커스텀 비즈니스 예외 클래스 (`BusinessException.java`)
`RuntimeException`을 상속받아 우리가 정의한 `ErrorCode`를 담을 수 있는 커스텀 예외를 생성합니다. 
필요에 따라 클라이언트에게 보내는 메시지와 서버 로그에만 남길 상세 메시지를 분리하여 받을 수 있도록 생성자 오버로딩을 활용하였습니다.

```java
@Getter
public class BusinessException extends RuntimeException {
    private final ErrorCode errorCode;
    private final String serverMessage; 

    public BusinessException(ErrorCode errorCode) {
        super(errorCode.getClientMessage());
        this.errorCode = errorCode;
        this.serverMessage = errorCode.getClientMessage();
    }

    public BusinessException(ErrorCode errorCode, String serverMessage) {
        super(errorCode.getClientMessage());
        this.errorCode = errorCode;
        this.serverMessage = serverMessage;
    }
}
```

<br>

### 4. 전역 예외 처리기 (`GlobalControllerAdvice.java`)
`@RestControllerAdvice`를 통해 전역에서 발생하는 예외를 낚아채서 처리합니다. 
>예: 비즈니스로직[예외발생] -던짐-> 컨트롤러 -던짐-> DispatcherServlet -낚아챔-> @RestControllerAdvice

여기서 각각의 예외 타입(`@ExceptionHandler`)에 따라 어떻게 로깅을 남기고 어떤 `ErrorResponse`를 만들어 HTTP Status와 함께 응답할지 결정합니다.

```java
@Slf4j
@RestControllerAdvice
public class GlobalControllerAdvice {

    // 1. 우리가 정의한 비즈니스 로직 예외 처리
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ErrorResponse> handleBusinessException(BusinessException e) {
        // 서버에는 상세 로깅
        log.error("[BusinessException] code={}, serverMessage={}", e.getErrorCode().getCode(), e.getServerMessage(), e);
        
        // 클라이언트에는 약속된 규격의 에러 응답 반환
        ErrorResponse response = ErrorResponse.from(e.getErrorCode());
        return new ResponseEntity<>(response, e.getErrorCode().getHttpStatus());
    }

    // 2. @Valid, @Validated를 통한 입력값 검증 실패(예: 이메일 형식 오류 등) 예외 처리
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ErrorResponse> handleValidationException(MethodArgumentNotValidException e) {
        // 여러 필드 에러를 하나의 문자열로 결합하여 클라이언트에게 명확한 원인 제공
        String message = e.getBindingResult()
                .getFieldErrors()
                .stream()
                .map(fieldError -> fieldError.getField() + ": " + fieldError.getDefaultMessage())
                .collect(Collectors.joining(", "));

        log.warn("[ValidationException] {}", message);
        ErrorResponse response = new ErrorResponse(ErrorCode.INVALID_INPUT_VALUE.getCode(), message);
        return new ResponseEntity<>(response, HttpStatus.BAD_REQUEST);
    }
    
    // 기타 Exception (ConstraintViolationException 등) 핸들링 생략...
}
```

<br>

## 3. 이 패턴의 장점 (비즈니스 로직 적용 시)

컨트롤러나 서비스 계층에서 데이터 검증 실패나 비즈니스 룰 위반 시, 단순히 에러 코드만 담아 커스텀 예외를 `throw` 하면 모든 응답 처리가 끝납니다.

**[적용 예시]**
```java
public void checkEmailDuplicate(String email) {
    if (memberRepository.existsByEmail(email)) {
        throw new BusinessException(ErrorCode.DUPLICATE_EMAIL);
    }

    // 예외만 던지면 GlobalControllerAdvice가 낚아채서 
    // 409 Conflict 와 함께
    //  {
    //    "code": "AUTH_001",
    //    "message": "이미 사용 중인 이메일입니다."
    //   }
    // 형태의 JSON 응답을 만들어줍니다.
}
```

<br>

1. 비즈니스 로직에는 에러 응답을 만들거나 로깅하는 `try-catch` 코드가 사라져 온전히 비즈니스 로직에 집중할 수 있도록 하였습니다.
2. 클라이언트는 언제나 예측 가능한 표준화된 구조(`ErrorResponse`)의 JSON 에러 데이터를 받을 수 있습니다.
3. 에러 내용(`ErrorCode`)이 한 곳에서 Enum으로 관리되므로, 시스템 내 어떤 에러들이 존재하는지 한눈에 파악하기 쉽고 확장 및 유지보수가 매우 편리해졌습니다.
>혹시라도 새로운 예외가 생긴다면 그저 일원화한 예외 코드 클래스에 추가해주고 해당 비즈니스 로직에서 던지기만 해주면됨<br>(물론 어드바이스에서 정의 하지 않은 예외일 시에는 처리할수있도록 해주어야함)


## +추가 정보

어쨋거나 @RestControllerAdvice는 비즈니스 로직에서 발생한 예외를 DispatcherServlet의 요청으로 낚아채 처리를 하는건데,
<br>
스프링시큐리티의 Filter 또는 서블릿 Filter에서 예외가 터진경우에는 @RestControllerAdvice는의 범위를 벗어나 잡지 못하기때문에 별도의 커스텀 필터에서 처리르 해야합니다.

<br>

또한 최근에는 Spring 6 / Boot 3부터 내장된 `ProblemDetail` 클래스를 사용하는 방식을 사용하기도 하는데 이는 `ErrorResponse`를 직접 만들지 않고 표준 스펙(type, title, status, detail, instance)을 그대로 사용합니다.

**[예시]**
```java
@ExceptionHandler(BusinessException.class)
public ProblemDetail handleBusinessException(BusinessException e) {
    ProblemDetail pd = ProblemDetail.forStatus(e.getErrorCode().getHttpStatus());
    pd.setDetail(e.getMessage());
    return pd;
}
```