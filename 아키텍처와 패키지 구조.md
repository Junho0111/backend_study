# 아키텍처와 패키지 구조(MVC, 계층형, DDD) 

* MVC / 전통적 계층형(Layered) 아키텍처란 기술적인 역할(Controller, Service, Repository 등)을 기준으로 계층을 나누는 방식
* DDD(Domain Driven Design) 아키텍처란 비즈니스 도메인(기능)을 중심으로 시스템의 경계를 나누는 도메인 주도 설계 방식

<br>

# 문제

웹 애플리케이션을 개발할 때 전통적인 MVC 패턴이나 계층형 아키텍처를 사용하여 패키지를 구성하는 경우가 많습니다. 
> 최상단에 `controller`, `service`, `repository`, `entity` 등의 패키지를 두고, 모든 기능의 클래스들을 역할에 맞춰 각 패키지에 모아두는 방식.

이렇게 기술적 역할에 따라 패키지를 나누게 되면, 초기에는 코드를 배치하기 쉽고 직관적이지만 프로젝트의 규모가 커지면서 문제가 생기게됩니다. <br>
만약 회원(Member)과 관련된 전체 코드를 수정해야 할 때, 개발자는 `controller`, `service`, `repository`, `entity` 패키지를 수시로 위아래로 오가며 코드를 찾아야 하는 불필요한 리소스가 발생합니다. 또한, <br>
기능 간의 결합도가 높아져 특정 기능만 떼어내기 매우 어려워집니다.

그럼 비즈니스 로직을 한눈에 파악하고 응집도를 높이려면 어떻게 해야 할까요?

```text
1. 전통적인 MVC / 계층 기반 패키징 
src/main/java/com/Architecture
 ├── controller
 │   ├── MemberController
 │   └── OrderController
 ├── service
 │   ├── MemberService
 │   └── OrderService
 ├── repository
 │   ├── MemberRepository
 │   └── OrderRepository
 └── entity
     ├── Member
     └── Order

```

<br>

# 해결 과정

도메인 주도 설계 기반 패키징 적용 <br>
기술적 계층이 아닌, 회원, 주문 등 비즈니스 도메인을 최상단 폴더로 끌어올려 관련 코드를 한곳에 모읍니다.

```text
2. 도메인 주도 설계 기반 패키징
src/main/java/com/Architecture
 ├── member
 │   ├── MemberController
 │   ├── MemberService
 │   └── MemberRepository
 ├── order
 │   ├── OrderController
 │   ├── OrderService
 │   └── OrderRepository

```

<br>

DDD 스타일의 내부 계층 분리 적용 <br>
모아둔 도메인 패키지 내부를 DDD 아키텍쳐에 맞춰 4가지 계층으로 명확히 분리합니다.

```text
3. DDD 아키텍처 기반 패키징
src/main/java/com/Architecture
 └── member
     ├── ui                  // 사용자 요청 처리 (Controller, DTO)
     ├── application         // 흐름 제어, 트랜잭션 관리 (Service)
     ├── domain              // 핵심 비즈니스 로직, 모델 (Entity, Repository 인터페이스)
     └── infrastructure      // DB 등 외부 시스템 통신 (Repository 구현체)

```

<br>

# 해결

비즈니스 도메인을 기준으로 코드를 모아둔 덕분에, 이제 회원과 관련된 로직을 파악할 때 또는 회원과 관련된 이슈가 생기면 `member` 패키지 하나만 열어보면 됩니다.

<br>

이렇게 하므로써 시스템을 기술적인 역할이 아닌 도메인을 중심으로 나누어 위의 문제를 해결하였으며, <br> 특정 도메인의 코드를 수정할 때, 다른 패키지를 돌아다닐 필요가 없게되었습니다. <br>
또한 나중에 특정 기능(예: 회원 관리 기능)만 따로 분리하여 MSA로 확장하기에도 훨씬 수월해집니다.

<br>

# 패키지 명과 의미
다른 사람들의 프로젝트 코드를 보다보면 분명히 DDD로 이루어져있는데 각각의 도메인의 세부 패키지명이 다른 경우가 있습니다.

<br>

이는 DDD 용어(`ui`, `application` 등) 대신, 자바 백엔드 개발자에게 친숙한 어노테이션 이름(`controller`, `service` 등)을 그대로 패키지명으로 사용하는 방식입니다.

```text
com.Architecture
 └── member
     ├── controller       (DDD의 ui 역할)
     ├── dto              (DDD의 ui 역할)
     ├── service          (DDD의 application 역할)
     ├── entity           (DDD의 domain 역할)
     └── repository       (DDD의 domain/infrastructure 역할)

```

<br>
<br>

# 그래서 어떻게 나누어야 하는가? 

실무에서는 프로젝트 규모와 도메인 복잡도에 따라 두 방식을 실용적으로 혼용한다고 합니다.

<br>

## 1. 기본 베이스는 '도메인(기능)별 패키징'

최상단에 `controller`, `service` 폴더를 두는 전통적인 MVC 아키텍쳐 방식은 이제 거의 쓰지않는다고 합니다. <br>
현재는 `member`, `order`, `payment` 등 도메인 별로 최상단 폴더를 두는 DDD가 일반적이라고 합니다.

<br>

## 2. 일반적인 서비스 (어드민, 간단하고 단순한 CRUD)

비즈니스 로직이 무겁지 않은 곳은 `controller`, `service`, `repository`의 패키지 명을 적극 사용합니다. <br>
이는 직관적이고 빠르게 개발할 수 있는 방식이 효율적이기 때문입니다.

<br>

## 3. 복잡하고 규모가있는 서비스 (결제, 주문, 정산 등)

로직이 복잡하고 변경이 잦은 도메인은 DDD 구조(`ui`, `application`, `domain`, `infrastructure`)와 헥사고널 아키텍처를 도입합니다. <br>
이는 프레임워크나 DB 기술에 종속되지 않고 순수한 비즈니스 로직으로 남겨두기 위함이라고 합니다.

<br>

# 정리

* 현재는 DDD(도메인 주도 설계)가 일반적이다.
* 도메인의 상세 패키지명 또는 아키텍쳐는 현재 진행하고 있는 프로젝트의 규모나 복잡도에 따라 정해야한다.